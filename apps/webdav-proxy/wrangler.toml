name = "webdav-proxy"
main = "src/worker.ts"
compatibility_date = "2026-02-24"

# 禁用所有默认路由配置以确保透明代理纯粹性
workers_dev = true

[vars]
# 在此处填写你的 WebDAV 默认后端地址，或在 Cloudflare Dashboard / Github Actions Secrets 中配置
# WEBDAV_URL = "https://your-backend-webdav.com/"
# WEBDAV_DIR = "/books"
